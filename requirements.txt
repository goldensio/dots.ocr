# NumPy 1.26.4 for PyTorch 2.1.2 compatibility (NumPy 2.x has breaking changes)
# IMPORTANT: Install NumPy first before other packages to prevent version conflicts
numpy==1.26.4

# PyTorch with CUDA 11.8 support
# Pin to version 2.1.x for stable CUDA 11.8 builds
torch==2.1.2
torchvision==0.16.2

# RunPod Serverless
runpod

# streamlit
gradio
# gradio_image_annotation
PyMuPDF
openai
qwen_vl_utils
transformers==4.40.0
huggingface_hub
modelscope
# flash-attn==2.8.0.post2  # to speed up inference need flash-attn
accelerate>=0.25.0
cairosvg

# API dependencies - NOT NEEDED FOR SERVERLESS
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# python-multipart>=0.0.6
