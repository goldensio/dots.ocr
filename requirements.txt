# NumPy 2.1.2 for PyTorch 2.5+ compatibility
# IMPORTANT: Install NumPy first before other packages to prevent version conflicts
numpy>=2.1.0

# PyTorch with CUDA 12.4 support for RTX 5090 (sm_120 compute capability)
# PyTorch 2.5+ required for CUDA 12.x and sm_120 support
torch==2.5.1
torchvision==0.20.1

# RunPod Serverless
runpod

# streamlit
gradio
# gradio_image_annotation
PyMuPDF
openai
qwen_vl_utils
transformers>=4.46.0
huggingface_hub
modelscope
# flash-attn==2.8.0.post2  # to speed up inference need flash-attn
accelerate>=0.25.0
cairosvg

# API dependencies - NOT NEEDED FOR SERVERLESS
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# python-multipart>=0.0.6
