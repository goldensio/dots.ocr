# streamlit
gradio
# gradio_image_annotation
PyMuPDF
openai
qwen_vl_utils
transformers==4.57.6
huggingface_hub
modelscope
# flash-attn==2.8.0.post2  # to speed up inference need flash-attn
accelerate
cairosvg

# NumPy 1.x for PyTorch 2.1.2 compatibility (NumPy 2.x has breaking changes)
numpy<2.0.0

# PyTorch with CUDA 11.8 support
# Pin to version 2.1.x for stable CUDA 11.8 builds
torch==2.1.2
torchvision==0.16.2

# RunPod Serverless
runpod

# API dependencies - NOT NEEDED FOR SERVERLESS
# fastapi>=0.104.0
# uvicorn[standard]>=0.24.0
# python-multipart>=0.0.6